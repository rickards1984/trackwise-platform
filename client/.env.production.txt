# Required for Vercel Build
VITE_API_BASE_URL=https://trackwise-platform-production.up.railway.app/

# Used for any display or CORS logic if needed
FRONTEND_URL=https://trackwise-platform.vercel.app/

# Optional - if you're exposing OpenAI etc. to frontend (be careful)
VITE_OPENAI_API_KEY=sk-...    # Only if intentionally client-side

# Add more VITE_ vars here if needed (must start with VITE_ to be accessible in Vite)
