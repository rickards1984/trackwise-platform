---
applyTo:
- "frontend/**"
---

# TrackWise – Frontend Instructions
- Scope: Only modify files under `frontend/**`.
- Node: 20.x. Build with `npm run build`.
- Tests: if Playwright config exists, run `npx playwright test`.
- Do not change backend or infra in this task.
- For broken builds, start by fixing ESLint/TypeScript errors, then get `npm run build` green.
